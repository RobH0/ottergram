<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('partials/head_links.ejs') %>
    <title>Ottergram</title>
</head>
<body>
    <header>
        <nav>
            <a href="/"><img class="standard-logo" src="/imgs/icons/otter-logo.svg" alt="Ottergram logo"></a>
            <a href="/login">Sign in</a>
        </nav>
    </header>
    <main>
        <h1>All posts</h1>

        <section class="feed-posts">
            <% for (let index = 0; index < allPosts.length; index ++){%>
                <div class="feed-post-container">
                    <div class="upper-feed-post">
                        <% if (allPosts[index].profilePic != ""){ %>
                            <a href="/login"><img class="user-profile-pic" src="<%= allPosts[index].profilePic %>" alt="user's post"></a>
                        <% } else { %>
                            <a href="/login"><img class="user-profile-pic" src="/imgs/icons/otter-logo.svg" alt="user's post"></a>
                        <% } %>
                        
                        <% if (allPosts[index].postByUsername == ""){ %>
                            <a href="/login"><h3>Unknown</h3></a>
                        <% } else { %>
                            <a href="/login"><h3><%= allPosts[index].postByUsername %></h3></a>
                        <% } %>
                    </div>
                    <div class="post-pic">
                        <a href="/login"><img src="<%= allPosts[index].img%>" alt="cute otter picture"></a>
                    </div>
                    <div class="lower-feed-post">
                        <div class="likes-comments">
                            <div class="feed-likes">
                                <a href="/login"><img src="/imgs/icons/like-heart-white.svg" alt="heart icon"></a>
                                <% if (allPosts[index].likes.length == null){ %>
                                    <span>0</span>
                                <% } else { %>
                                    <span><%= allPosts[index].likes.length %></span>
                                <% } %>
                            </div>
                            <div class="feed-likes">
                                <a href="/login"><img src="/imgs/icons/comment-black-outline.svg" alt="comment icon"></a>
                                <% if (allPosts[index].comments.length == null){ %>
                                    <span>0</span>
                                <% } else { %>
                                    <span><%= allPosts[index].comments.length %></span>
                                <% } %>
                            </div>
                        </div>
                        <div class="post-date">
                            <% if (allPosts[index].datePosted == null){ %>
                                <span>Posted along time ago, in a galaxy far, far away...</span>
                            <% } else { %>
                                <span><%= allPosts[index].datePosted %></span>
                            <% } %>
                        </div>
                    </div>
                </div>
            <% } %>
        </section>
    </main>
    <script src="/js/likeUnlike.js"></script>
</body>
</html>